{
  "name": "digitask-security-tests",
  "private": true,
  "scripts": {
    "pretest": "node -e \"for (const file of ['firestore.rules','storage.rules']) require('node:fs').copyFileSync('../../'+file, file)\"",
    "test": "firebase emulators:exec --project demo-digitask-security --config firebase.json --only firestore,storage \"node --test --test-concurrency=1 rules.test.cjs bank-workflow.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-admin": "12.7.0",
    "firebase-tools": "13.35.1"
  }
}
