{
  "name": "digitask-security-tests",
  "private": true,
  "scripts": {
    "pretest": "node -e \"require('node:fs').copyFileSync('../../firestore.rules', 'firestore.rules')\"",
    "test": "firebase emulators:exec --project demo-digitask-security --config firebase.json --only firestore \"node --test rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "13.35.1"
  }
}
